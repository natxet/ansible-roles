---

- name: All packages needed by webtier (not apache or php) are installed in their latest version
  action: apt pkg={{ item }} state=latest
  with_items:
    - imagemagick
    - memcached
    - sendmail
